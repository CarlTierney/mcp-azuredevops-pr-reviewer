# Azure DevOps Configuration
# Your Azure DevOps organization name
AZURE_DEVOPS_ORG=your-organization-name

# Default Azure DevOps project name
# This is used as the default project if not specified in tool calls
AZURE_DEVOPS_PROJECT=your-project-name

# Azure DevOps Personal Access Token (PAT)
# Create at: https://dev.azure.com/{your-org}/_usersSettings/tokens
# Required permissions: Code (Read & Write), Pull Request (Read & Write)
AZURE_DEVOPS_PAT=your-personal-access-token

# Your Azure DevOps email address
# This is used to filter PRs that need your review
AZURE_USER_EMAIL=your.email@example.com

# Optional: Default repository name (if you work primarily in one repository)
# AZURE_DEFAULT_REPOSITORY=your-repository-name

# Server Configuration (optional)
# MCP_SERVER_NAME=azure-pr-reviewer
# MCP_SERVER_VERSION=0.1.0

# Logging Configuration (optional)
# LOG_LEVEL=INFO
# LOG_FILE=azure_pr_reviewer.log

# Review Configuration
REVIEW_MODEL=claude-3-opus-20240229  # Claude model for reviews (opus recommended for best quality)
# DEFAULT_REVIEW_PROMPT_PATH=prompts/default_review_prompt.txt
# ENABLE_MIXED_FILE_TYPE_REVIEW=true
MAX_FILES_PER_REVIEW=5000  # Maximum files to analyze per review
MAX_TOTAL_SIZE_GB=2  # Maximum total size in GB

# Working Directory Configuration (REQUIRED for full context analysis)
# This directory is used to clone repositories for complete file analysis
# Ensure this directory exists and has write permissions
# Windows example: D:/temp/pr-review or D:\\temp\\pr-review
# Linux/Mac example: /tmp/pr-review or ~/temp/pr-review
WORKING_DIRECTORY=D:/temp/pr-review
AUTO_CLEANUP=true  # Automatically clean up working directory before each analysis

# Cache Configuration (optional)
# ENABLE_PR_CACHE=true
# CACHE_TTL_SECONDS=300

# Rate Limiting (optional)
# AZURE_API_RATE_LIMIT=100
# RATE_LIMIT_WINDOW_SECONDS=60

# Feature Flags (all enabled by default)
# ENABLE_SECURITY_DETECTION=true
# ENABLE_PACKAGE_SCANNING=true
# ENABLE_TEST_ENFORCEMENT=true
# ENABLE_FULL_CONTEXT_ANALYSIS=true
# ENABLE_MARKDOWN_OUTPUT=true